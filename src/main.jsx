import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import MotherFile from './Pages/MotherFile/MotherFile'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MotherFile></MotherFile>
  </React.StrictMode>,
)
